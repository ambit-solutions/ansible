---
# deploy.yml

- hosts: localhost
  connection: local
  gather_facts: no
  roles:
    - role: terminateInstances

- hosts: localhost
  connection: local
  gather_facts: no
  roles:
    - role: launch
      name: ansible_aws_demo_2

- hosts: ansible_aws_demo_2
  roles:
    - deploy
    - nginx

- hosts: localhost
  connection: local
  gather_facts: no
  roles:
    - createAMI
    - createLC
    - createELB
    - createASG
